/**
 * Stable landmark ids. Shared between the skip link and the layout so the two
 * cannot drift apart.
 */
export const LANDMARK = {
  main: 'main-content',
  nav: 'primary-navigation',
  footer: 'site-footer',
} as const;
