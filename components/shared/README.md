# components/shared

Reserved. Populated in Sprint 02.

Boundary rule (see `docs/ARCHITECTURE.md`): a component lives in
`components/` only if it carries **no brand meaning**. Anything that encodes an
identity element — the Frame, the Source Line, the Triad, the Margin Voice —
belongs in `features/`.
